# 小红书招生 Agent · 工作流程（修订）

先分稿，再合流。两类稿件**分开录入、分开管理**，**同一套 AI 过敏感词 + 人工审核**之后，才能发布或进定时。

---

## 1. 两种稿（必须先分开）

### A. 每月固定招聘岗位文案

- 每个月都在招的岗位（如数控、质检等），有固定岗位库。
- 按「岗位 × 月份」出稿：本月招哪些岗、招到哪天、每天是否定时发。
- 岗位事实（学校、岗位名、是否住宿、年龄口径、对接方式）写在岗位档案里，本月文案引用，不每次从零编。
- 日更只换标题/封面/开头，**不改岗位事实**。

### B. 偶发的新人工文案

- 临时写的新内容：新活动、新话术、业务随手写的招聘稿等。
- 没有固定岗位档案，或尚未入库。
- 一次一稿：写完就进优化，审过可单发，也可「转成固定岗」下月复用。

两类在系统里是两个入口，列表分开，互不混成一条「活动」糊弄过去。

---

## 2. 统一后半段（两类完全一样）

```text
【A 固定岗】选岗位 + 本月文案/变体  ─┐
                                    ├──► AI 优化敏感词 ──► 过审稿
【B 偶发】粘贴/上传人工新稿        ─┘
                                              │
                                              ▼
                                         人工审核
                                              │
                          通过 ──► 可立即发 / 可进该类自己的发送计划
                          退回 ──► 回对应类型改，再送 AI 或直接改过审稿
                          作废 ──► 本条结束
```

- **没有「通过」，不能发、不能进每天定时。**
- A 的定时只挂 A 里已通过的稿；B 的单发只挂 B 里已通过的稿。
- AI 不区分 A/B，只吃「待优化原文 + 风险规则」。

---

## 3. 分类型怎么走（前半段）

### A. 每月固定招聘岗位

```text
岗位库（长期）
  岗位名称、技能方向、学校、住宿、年龄、对外口径
        │
        ▼
本月招聘计划
  本月开哪些岗、期次、起止日期、每天几点发、用哪几条已审变体轮换
        │
        ▼
本月文案（母稿 + 日更变体）
        │
        ▼
进入「统一：AI 优化敏感词 → 人工审核」
        │
        ▼
通过后：写入「A 类发送日历」（每天定时）
```

月初：从岗位库勾选本月在招岗位 → 生成或沿用文案 → **每条都重新过 AI + 人审**（规则变了必须重审）→ 批准本月每日发送。

### B. 偶发新人工文案

```text
人工写新稿（可直接贴初稿）
        │
        ▼
进入「统一：AI 优化敏感词 → 人工审核」
        │
        ▼
通过后：立即发 或 指定时间发一次
        │
        └─ 可选：审核人点「转入岗位库」→ 变成 A 类下月固定招
```

B 默认不是每天循环，除非人明确转成 A 并挂上每日计划。

---

## 4. AI 优化敏感词（两类共用）

对原文扫描本地风险词（高/中/低），给出：

- 命中列表  
- 一版过审稿  
- 左右对照：改了哪几处  

处理原则（与现有规则一致）：

- 高危：必须改掉才能进人审通过（涉军承诺、未成年就业导向、确定性入职、百分百就业等）  
- 中危：改写或加限定（如薪资以合同为准）  
- 低危：标给审核人  

AI **优化的是过审**，不是把原意用隐语保住。人审看到的是过审稿，不是「换皮还是原承诺」。

学校等事实以档案/原文为准：沈阳佳旭培训教育中心、不包住宿、年满 16 周岁等，优化时保留。

---

## 5. 人工审核（两类共用同一工作台，带类型标签）

每条待审显示：**类型 A 或 B**、岗位/期次（A 有）、原文、过审稿、风险标签。

| 操作 | 结果 |
|------|------|
| 通过 | A 可进本月每日表；B 可立即发或单次定时 |
| 退回 | 写原因，回到该类编辑，再跑 AI 或手改过审稿 |
| 作废 | 本条作废，A 不影响岗位库本身 |

审核队列可筛选：只看 A / 只看 B / 只看高危改动多的。

---

## 6. 通过之后（仍按类型分）

| | A 固定岗 | B 偶发 |
|--|----------|--------|
| 发送 | 本月每日定时，变体轮换 | 立即或指定时刻发 1 次 |
| 重复发 | 每天换标题/封面/首段，事实同岗位档案 | 默认不连发 |
| 数据 | 按岗位、按变体看浏览 | 按单篇看浏览 |
| 评论 | 同一套已批准 FAQ 建议回复，人确认后发 | 同左 |

评论不机器人互评；FAQ 与过审口径一致（住宿、年龄、学校、面试/合同以企业为准）。

---

## 7. 页面怎么对应这两种稿

1. **岗位库（仅 A）** — 固定招聘岗位档案  
2. **本月固定招** — 选岗、本月文案、每日日历  
3. **偶发新稿（仅 B）** — 粘贴原文、送优化  
4. **统一审核台** — 标签 A/B，通过/退回/作废  
5. **发送与数据** — 按类型筛选  
6. **评论箱** — 按已发笔记，不按 A/B 拆两套话术（话术统一批准）

---

## 8. 一条稿的状态

```
A：岗位已建 → 本月文案已写 → 待 AI → 待人审 → 已通过 → 已排期 → 已发布
B：新稿已写 → 待 AI → 待人审 → 已通过 → 已发布（或单次定时）

任意步可：退回、作废
规则变更：已通过但未发完的 A 变体 → 需复审
```

---

## 9. 和旧方案差在哪

| 旧 | 现 |
|----|----|
| 用「活动 / 临时稿」混着讲 | **先定 A 固定招聘岗 / B 偶发人工稿** |
| 固定招像附属功能 | A 是主业务：岗位库 + 每月计划 + 每日发 |
| 偶发和日更都叫变体 | B 是独立入口，不默认每天发 |
| 后半段流程散 | **AI 敏感词 + 人审只有一条，A/B 都必须走** |

---

## 10. 待你确认

- A 类现在有几个固定岗位、是否每月同一套岗。  
- A 每天几点发、一个号一天几条。  
- B 是否允许「审过转入 A」。  
- 审核是不是你一个人。
